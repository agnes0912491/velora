import { TranslationConfig } from "@tengra/language";

const config: TranslationConfig = {
    locales: ["tr", "en"],
    defaultLocale: "tr",
    translationsPath: "../../translations/velora",
};

export default config;
