import { TranslationConfig } from "@tengra/language";

const config: TranslationConfig = {
    locales: ["tr", "en"],
    defaultLocale: "tr",
    translationsPath: "../../translations/apps/velora-translations",
};

export default config;
